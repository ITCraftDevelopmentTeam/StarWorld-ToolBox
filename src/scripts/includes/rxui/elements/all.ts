import ELEMENTS_TEXT from "./text";
import ELEMENTS_ALIAS from "./alias";
import ELEMENTS_POPOVER from "./popover";
import ELEMENTS_SCRIPT from "./script";
import ELEMENTS_ACTIVABLE from "./activable";

export let all = {
    ...ELEMENTS_TEXT,
    ...ELEMENTS_ALIAS,
    ...ELEMENTS_POPOVER,
    ...ELEMENTS_SCRIPT,
    ...ELEMENTS_ACTIVABLE
}

export default all;
